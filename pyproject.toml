[project]
name = "comptine-dev"
version = "0.1.0"
description = "Development dependencies for Comptine"
requires-python = ">=3.10"
dependencies = [
    "pre-commit>=4.0.0",
    "clang-format==21.1.7",
    "pyyaml>=6.0",
]
